# Your CLI Is Already a Tool Contract

*Chapter stub — the article version is queued at Mesh Intelligence.*

A well-behaved command-line utility — typed flags, stdin/stdout, exit
codes — already satisfies most of what a tool declaration needs. This
chapter maps CLI conventions onto the tool contract.
