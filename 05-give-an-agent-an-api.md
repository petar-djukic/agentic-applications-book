# How to Give an Agent an API

*Chapter stub — the article version is queued at Mesh Intelligence.*

REST endpoints become agent tools by declaration: typed inputs, outputs,
side effects. This chapter covers wiring an existing API into an agent
loop without writing glue code.
