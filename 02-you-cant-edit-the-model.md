# You Can't Edit the Model

*Chapter stub — the article version is queued at Mesh Intelligence.*

Every mechanism you rely on in an agentic application must live outside
the model, because the model is the one component you cannot patch. This
chapter makes the case for agents as configuration rather than code:
declare the behavior, verify the declaration, and let one runtime
interpret whatever loop you hand it.
